name: Build
on:
  push:
    paths:
      - 'js/list.min.js'
      - 'js/main.js'
      - 'index_dev.html'
      - 'Makefile'
      - '!.github/workflows/**'
      - '.github/workflows/build.yml'
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v3.5.2
    - name: Build arquivos
      run: make
    - name: Commit builds
      uses: EndBug/add-and-commit@latest
      with:
        add: '["index.html", "js/all.min.js"]'
        author_name: pxlcmrd
        author_email: raphael.toledo@outlook.com
        message: 'Build'

